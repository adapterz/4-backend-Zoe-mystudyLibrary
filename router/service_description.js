// 서비스 설명 라우터
const express = require("express");
const router = express.Router();
const controller = require("../controller/controller_service_description");
// 아직 작성할거 따로x -> 기능이 없는 페이지
// 모듈화
module.exports = router;
